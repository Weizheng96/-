# 关键证据 — Linux kernel bonding + mlx5 LAG offload（CT-2）

> 候选标识：`02-linux-kernel-bonding-mlx5-lag` | 当前判定：第 3 档（强候选）+ 强现有技术 caveat | [`_verdict.md`](_verdict.md)

## 一、F# 证据原文 + 网页链接

适用独权：权 1 / 11 / 23 / 33 / 34 / 35 / 36

| F# | 证据原文 | 网页链接 | 命中 |
|---|---|---|---|
| F1 | Linux kernel bonding driver 支持任意 N 张 NIC 聚合（`Documentation/networking/bonding.rst`） | [kernel.org bonding](https://www.kernel.org/doc/Documentation/networking/bonding.txt) | **字面命中** |
| F2 | "Mode 4 (802.3ad): IEEE 802.3ad Dynamic link aggregation. Creates aggregation groups that share the same speed and duplex settings. Utilizes all slaves in the active aggregator..." | [kernel.org bonding.rst](https://www.kernel.org/doc/Documentation/networking/bonding.txt) | **字面命中** |
| F3 | bond master interface（如 bond0）+ slaves 抽象 — bond master 是"第一端口"等价物 | 同上 | **字面命中** |
| F4 | NIC miss → kernel netdev stack → upcall to userspace | Linux kernel network stack 标准 | **字面命中** |
| F5 | mlx5 LAG offload — `drivers/net/ethernet/mellanox/mlx5/lag/` 跨 PF LAG 在硬件 e-switch 同步 offload | [mlx5 LAG source](https://elixir.bootlin.com/linux/latest/source/drivers/net/ethernet/mellanox/mlx5/core/lag) | **字面命中候选**（mlx5 LAG 在硬件层做 LAG offload；但单卡 dual-PF 默认拓扑下工作在 PF 级而非跨独立 NIC 卡）|

## 二、已检查文档清单

| # | 文档名 | 内容摘要 | 网页链接 | 简要评价 |
|---|---|---|---|---|
| 1 | Linux kernel bonding.rst | bonding driver 完整文档（modes 0-6 + 802.3ad LACP）| [kernel.org bonding](https://www.kernel.org/doc/Documentation/networking/bonding.txt) | F1-F4 字面命中 |
| 2 | mlx5 LAG driver 源码（`drivers/net/ethernet/mellanox/mlx5/core/lag/`）| Mellanox/NVIDIA 实现的跨 PF LAG offload | [Bootlin mlx5 LAG](https://elixir.bootlin.com/linux/latest/source/drivers/net/ethernet/mellanox/mlx5/core/lag) | F5 字面命中候选 |
| 3 | mlx5 driver Linux kernel git log `--before=2020-10-31` | **现有技术 caveat 关键证据** | [Linux kernel git log](https://git.kernel.org/) | **未深查**——建议法务本地 clone + `git log --author="@mellanox.com" --before=2020-10-31 -- drivers/net/ethernet/mellanox/mlx5/` |
| 4 | NVIDIA Mellanox 在 USPTO 2017-2020 同主分类专利墙 | 现有技术抗辩潜在依据 | [USPTO Patent Public Search](https://ppubs.uspto.gov/pubwebapp/) | **未深查**——建议法务通过 IncoPat 补查 |
| 5 | ConnectX-5 / 6 historical user manual（2017-2020）| NVIDIA NIC 早期版本是否支持本专利两层 LAG | NVIDIA 客户网站 / 历史文档 | **未深抓取** |
| 6 | OIN / Linux Foundation patent commitment 数据库 | 华为是否做过 patent pledge | [OIN](https://openinventionnetwork.com/) | **工具能力受限**——建议法务通过 IPR 律师补查 |
