# 13-dingtalk-rtc 检索留痕（_sources.md）

## Phase 1 — react 粗筛 query 留痕
1. WebSearch：`钉钉 DingTalk 音视频会议 弱网 FEC 冗余 抗丢包 自适应`
   - 结果：钉钉 Real "自适应网络优化技术"（检测网络不佳自动调整视频传输参数）；其余为声网/百度/通用 ULPFEC 科普。无钉钉自有 FEC 机制细节。
2. WebSearch：`钉钉 RTC FEC 冗余 网络状态 业务类型 QoS 自研 阿里云`
   - 结果：全部命中阿里云 RTC QoS 弱网系列（变分辨率编码、屏幕共享编码优化）+ 通用 WebRTC FEC 科普；无钉钉专属传输栈披露。
3. WebSearch：`钉钉 音视频 技术架构 实时传输 抗丢包 算法 自研 博客 技术揭秘`
   - 结果：钉钉音视频算法"开放升级"（远场拾音等音频算法，应用于生态硬件）+ 钉钉后端单元化架构；无发送侧 FEC 冗余调度披露。
4. WebSearch：`钉钉会议 实时音视频 基于阿里云 RTC 传输栈 集成`
   - 关键发现：阿里云官方表述"钉钉会议和阿里云音视频通信 RTC 共用一套底座"；"AliRTC 3.0 是基于钉钉音视频能力开发的新一代 RTC，沉淀了钉钉海量用户技术经验"。即钉钉音视频传输栈 = 阿里云 RTC 同一套底座（共建）。

## Phase 2 — react 深抓
- WebFetch：https://www.dingtalk.com/qidian/page-oxZyNlCZ.html （钉钉 Real 产品页）
  - 仅取到 "自适应网络优化技术：当检测到网络状况不佳时，软件会自动调整视频传输参数以保证画面连续性"。无 FEC / 前向纠错 / 冗余 / 三输入冗余量 / 时延→传输总时间→调度链 的任何原文。

## 关键架构备注
- 钉钉自身公开披露集中在**音频算法**（降噪、远场拾音）与**通用"自适应网络优化"**口径，发送侧 FEC 冗余量/调度的传输内核**未单独披露**。
- 据阿里云官方，钉钉会议与阿里云 RTC **共用同一底座**（AliRTC 3.0 基于钉钉音视频能力开发）。因此钉钉传输栈的 F1-F5 实质等同于候选 09-aliyun-artc 的判定面，但**不能把阿里云 RTC 内部 verbatim 直接计为钉钉自有披露的字面/等同命中**——本候选按钉钉自身公开材料判定。
- 与 09-aliyun-artc 判定保持一致：阿里云侧也仅 F5 等同命中、F1-F4 资料不足，落第 4 档。
